import colors from "tailwindcss/colors"

const primary = 'zinc'
const secondary = 'fuchsia'

export default {
    bottomTabColor: colors[primary][950],
    pageBackgroundColor: colors[primary][950],
    containerBackgroundColor: colors[primary][800],
    containerBackgroundColorSecondary: colors[primary][700],
    titleColor: colors[primary][50],
    color: colors[primary][100],
    secondaryColor: colors[primary][500],
    callToActionBackground: colors[secondary][800],
    secondaryCallToActionBackground: colors[primary][600],
    callToActionTextColor: colors.neutral[100],
    borderColor: colors[secondary][800],
}
